/**
 * Products Initialization Plugin - DISABLED
 * 
 * We now use Pinia stores for product management.
 * This plugin is kept for reference but disabled.
 * 
 * See: woonuxt_base/app/stores/products.ts and woonuxt_base/app/stores/product.ts
 */

// DISABLED: We now use Pinia stores instead of static JSON
export default defineNuxtPlugin((nuxtApp) => {
  console.log('[Products Plugin] ℹ️ Disabled - Using Pinia stores now');
});







